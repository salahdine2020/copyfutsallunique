import 'package:flutter/material.dart';

class ChatScreenUser extends StatelessWidget {
  const ChatScreenUser({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("connected yo"));
  }
}
